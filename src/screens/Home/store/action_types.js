import Constants from '../constants';
const { NAME } = Constants;

export const FETCHING_TODOS = `${NAME}/FETCHING_TODOS`;
export const FETCH_TODOS = `${NAME}/FETCH_TODOS`;
export const TOGGLE_CHECK_TODO = `${NAME}/TOGGLE_CHECK_TODO`;
export const CHANGE_TEXT = `${NAME}/CHANGE_TEXT`;
export const SUBMIT_TODO = `${NAME}/SUBMIR_TODO`;
